const defaultTheme = require('tailwindcss/defaultTheme');
module.exports = {
    darkMode: 'class',
    theme: {
        extend: {
            fontFamily: {
                // 'comf': [],
                // 'lora': [],
                // 'nun': [],
                // 'libre': [],
                'monte': ["Montserrat", "sans-serif"],
                // sans: [],
            }
        }
    }
}